const user = {
  create: context => context.orm['default'],
}

const create = context => {
  const m = {
    models: {
      'namespace-app': {
        PluralName: {},
      },
    },
  }

  return {}
}
